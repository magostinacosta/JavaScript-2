let nueve = 9
let diez = 10
let once = 11
for (let n = 1; n <= 9; n++) {
    if (n == 1) {
        document.write(`<h2>Tabla del 11</h2>`)
    }
    document.write(`<p>${once}x${n}=${once*n}</p>`)
}
for (let n = 1; n <= 9; n++) {
    if (n == 1) {
        document.write(`<h2>Tabla del 10</h2>`)
    }
    document.write(`<p>${diez}x${n}=${diez*n}</p>`)
}
for (let n = 1; n <= 9; n++) {
    if (n == 1) {
        document.write(`<h2>Tabla del 9</h2>`)
    }
    document.write(`<p>${nueve}x${n}=${nueve*n}</p>`)
}